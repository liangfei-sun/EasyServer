# EasyServer - Nginx 反向代理模块
# 数据路径使用 ${DATA_DIR} 变量，由管理引擎注入

services:
  nginx:
    image: nginx:stable
    container_name: easyserver-nginx
    network_mode: ${NGINX_NETWORK_MODE:-host}
    volumes:
      - ./conf.d:/etc/nginx/conf.d
      - ./ssl:/etc/nginx/ssl
      - ./log:/var/log/nginx
      - ./acme-challenge:/acme-challenge
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
